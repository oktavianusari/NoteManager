<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal">
	
    <!--  Sample Dashboard screen with Two buttons -->
    <Button android:id="@+id/btnRole"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Manajemen Role"
        android:layout_marginTop="25dip"/>
    
     <Button android:id="@+id/btnUser"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Manajemen User"
        android:layout_marginTop="25dip"/>
     
      <Button android:id="@+id/btnKategori"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Manajemen Kategori"
        android:layout_marginTop="25dip"/>
      
      <Button android:id="@+id/btnStatus"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Manajemen Status"
        android:layout_marginTop="25dip"/>
      
      <Button android:id="@+id/btnArtikelTest"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Manage Artikel TEST"
        android:layout_marginTop="25dip"/>
      
        <Button android:id="@+id/btnLogout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Logout"
        android:layout_marginTop="25dip"/>
    
  
</LinearLayout>